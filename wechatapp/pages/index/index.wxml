<!-- 模版 -->
<swiper class="section hero white" indicator-dots="{{true}}">
    <block wx:for="{{slides}}" wx:key="id">
        <swiper-item>
            <image src="{{item.image}}" />
            <view class="content centered">
                <view class="sub-header">{{item.sub_header}}</view>
                <view class="header">{{item.header}}</view>
                <view class="discription">{{item.description}}</view>
            </view>
        </swiper-item>
        <swiper-item>
            <image src="https://resources.ninghao.net/landrover/discover-1.jpg" />
            <view class="content centered">
                <view class="sub-header">Discovery</view>
                <view class="header">新一代发现者</view>
                <view class="discription">全尺寸七座SUV，接受预定</view>
            </view>
        </swiper-item>
        <swiper-item>
            <image src="https://resources.ninghao.net/landrover/discover-1.jpg" />
            <view class="content centered">
                <view class="sub-header">Discovery</view>
                <view class="header">新一代发现者</view>
                <view class="discription">全尺寸七座SUV，接受预定</view>
            </view>
        </swiper-item>
    </block>
</swiper>